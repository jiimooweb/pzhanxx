<view class="wallpaper-relative">
      <view class="labels">
        <button formType="submit" hoverClass="none" wx:for="{{tags}}" wx:key="{{index}}" data-id="{{item.id}}" data-name="{{item.name}}" catchtap="toPics">{{item.name}}</button>
      </view>
    </view>