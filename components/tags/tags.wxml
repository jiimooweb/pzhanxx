<view class="wallpaper-relative">
  <view class="labels">
    <view formType="submit" hoverClass="none" wx:for="{{tags}}" wx:key="{{index}}" data-id="{{item.id}}" data-name="{{item.name}}" catchtap="toPics">{{item.name}}</view>
  </view>
</view>